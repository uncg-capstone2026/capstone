This is will the backend stuff will go


--data fields that javier needs--
-- on clothing items I need these fields --
    type TEXT NOT NULL,
    color TEXT,
    pattern TEXT,
    material TEXT,
    season TEXT,
    formality TEXT,
    image_url TEXT,
    embedding VECTOR(768)
    -------------------------------
